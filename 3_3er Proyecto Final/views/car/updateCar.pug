html
    head 
        title Coder Merb
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous")
        link(rel="stylesheet" href="../../../css/updateProduct/updateProduct.css")
    body(class="container")
        h1 Actualizar videojuego en carrito
        |
        div(class="boxForm")
            form(onsubmit="return btnUpdateCar()")
                |
                input(type="hidden" id="id" value=`${product._id}`)
                input(type="hidden" id="name" placeholder="Nombre" class="mt-3 form-control form" value=`${product.name}`)
                input(type="hidden" id="description" placeholder="Descripción" class="mt-3 form-control form" value=`${product.description}`)
                input(type="hidden" id="code" placeholder="Código" class="mt-3 form-control form" value=`${product.code}`)
                input(type="hidden" id="urlPicture" placeholder="Ruta de Imagen" class="mt-3 form-control form" value=`${product.urlPicture}`)
                input(type="hidden" id="price" placeholder="Precio" class="mt-3 form-control form" value=`${product.price}`)
                label(for="amount" class="mt-5 h4") Cantidad que va a comprar
                input(type="text" id="amount" placeholder="Piezas" class="mt-3 form-control form" value=`${product.amount}`)
                |
                input(class="mt-3 btn btn-dark form-control button" type="submit" value="Modificar")
        |
        script(src="../../../apis/updateProductCar.js")